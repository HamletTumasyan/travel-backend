<div class="action">
  <h1> <%= title %> </h1>   

    <form action="/admin/<%= activeRoute %>/edit/<%= data.id %>" method="post">
        <label for="title">Title <span>*</span></label>
        <input type="text" id="title" name="title" value="<%= data.title %>" required>

        <button type="submit" class="submit">Update</button>

    </form>
</div>